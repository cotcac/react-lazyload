import React, { Component } from 'react';

class Lazy extends Component {  
    render() {
        return (
            <div>
                Im lazy component.
            </div>
        );
    }
}

export default Lazy;